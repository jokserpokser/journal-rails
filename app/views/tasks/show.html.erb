<%= link_to 'Back', category_tasks_path(@category) %>
<h2>Task Name: <%= @task.name %></h2>
<%= link_to 'Edit Task', edit_category_task_path(@category, @task) %>
<br/>
<%= button_to 'Delete Task', category_task_path(@category, @task), method: :delete %>
